# Plan & Status

Full phase rationale lives in DECISIONS.md and the original plan at
`~/.claude/plans/project-agentic-financial-onboarding-sharded-gray.md`. This file tracks
live status only — update the checkbox and add a one-line note as each phase closes.

Demo money-shot (build everything backward from this): two students, same university
(Texas A&M), different job situations — one has an on-campus job offer, one doesn't. The
copilot produces two different sequenced plans and explains *why* one is blocked on SSN
and the other isn't — shown as a staged timeline UI with per-step status, explanation text
toggleable between English and Hindi. (Voice cut 2026-08-15 — see DECISIONS.md.)

## Status

- [x] **Phase 1** — State schema, dependency-graph design, repo scaffold (~1.5 hrs) — done: backend (FastAPI + venv, boots, `/health` OK) and frontend (Next.js + Tailwind, builds clean) scaffolded; `StudentState` + `StepDefinition`/`PlanStep` schemas written; LangGraph skeleton (intake→resolver→planner stubs) compiles.
- [x] **Phase 2** — Deterministic resolver, the differentiator (~2.5–3 hrs) — done: `app/resolver.py` (5-step graph, recursive/memoized, order-independent, zero LLM calls), realistic SSN rule (job offer → letter → SSN chain) wired, `resolver_node` connected for real. Verified via `backend/scripts/verify_resolver.py` against the money-shot pair (Priya/Wei) + 2 edge cases — all assertions pass, root-cause reasons bubble up correctly.
- [x] **Phase 3** — RAG pipeline, two-bucket + metadata filtering (~2–2.5 hrs) — done: 8 curated docs (3 A&M, 3 Purdue, 2 general) with real sourced content (fee deadlines, SSN process, banking) scraped/researched via web search+fetch; Chroma collection with `university`/`visa_type`/`topic` metadata; `app/rag/retrieve.py` filters correctly. Verified via `backend/scripts/verify_rag.py` — same query returns different top doc per school, general bucket reachable from both, no cross-school contamination. A&M's Fall 2026 fee deadline (Aug 21) also fixed the Phase 2 placeholder date.
- [x] **Phase 4** — Intake loop + LangGraph orchestration glue (~2 hrs) — done: `app/intake/extractor.py` (forced-tool-use extraction), `app/intake/questions.py` (deterministic follow-up templates), `app/planner.py` (RAG-grounded explanation, respects resolver's status/reasons — never overrides them), LangGraph now has real conditional routing (ask-more vs. proceed to resolver→planner). **Provider switched mid-phase: Anthropic → Featherless AI** (free hackathon sponsor credits, `MiniMaxAI/MiniMax-M2.5`) — see DECISIONS.md. Modules import cleanly; **still not run live end-to-end** — needs `FEATHERLESS_API_KEY` in `backend/.env`. `scripts/verify_intake_e2e.py` and `scripts/chat_intake.py` are ready to run once the key is added.
- [x] **Phase 5** — Per-step artifacts + timeline UI, incl. EN/HI language toggle (~3–3.5 hrs) — built: `app/main.py` (CORS + `/personas`, `/personas/{id}/plan?lang=`, `/personas/{id}/artifacts/isss-email`, `/personas/{id}/artifacts/calendar.ics`), `app/artifacts.py` (deterministic ICS via `icalendar`, LLM-drafted ISSS email), `planner.py` batches all steps into one forced-tool-use call instead of one-per-step. UI restructured 2026-08-15 into a single active-account view + "Signed in as ▾" switcher (see DECISIONS.md); `StepCard` shows resolver reasons and LLM explanation as visually distinct blocks. **Live LLM path now fully verified (2026-08-15)** — Featherless credits never came through, swapped to Groq (`llama-3.3-70b-versatile`, see DECISIONS.md "LLM provider: Groq"). First live run surfaced and fixed two real bugs: a `KeyError` from the Phase 5c graph expansion never being reflected in `planner.py`'s topic map, and a genuine grounding failure (the ISSS email drafter fabricated "I have a job offer" for a student who doesn't, now gated on resolver status like "mark done" is). Real English + Hindi explanations, a truthful email draft, and the blocked-persona 409 path all confirmed live and screenshotted.
- [x] ~~Phase 6 — Voice + multilingual~~ — **cut 2026-08-15**, see DECISIONS.md. Freed ~2.5–3 hrs reallocated to Phase 5 polish and Phase 8 buffer below, not spent on new scope.
- [x] **Phase 7** — Minimal eval set (~1 hr) — done: `backend/scripts/eval_personas.py`, hand-verified expected status per step across the money-shot pair + 2 edge cases (20 checks total), no LLM/API key needed since it evaluates the deterministic resolver, not the explanations. **20/20 passed** on first run — screenshot-able summary line, genuinely pitch material. Done out of order (before finishing Phase 5's live-LLM verification) since it had zero dependency on the missing API key.
- [x] **Phase 5b (unplanned, while blocked on the API key)** — UI redesign: warm/light/friendly restyle, Home turned into a personal progress dashboard, new Tracker/FAQs/Docs pages behind a shared nav, local document store (`app/documents.py`, upload/list/view, no LLM), client-side personal checklist ("add a new task", explicitly not resolver-wired). All routes typecheck, lint clean, and build; screenshotted. Document upload verified with a real Playwright browser run (select doc type → choose file → upload → appears in list) — full round trip confirmed, not just typechecked. See DECISIONS.md for the full breakdown incl. a real Next 16 lint-rule fix (`react-hooks/set-state-in-effect`).
- [x] **Phase 5c (unplanned, while blocked on the API key)** — Dependency subtasks + real "mark done" + deterministic recommend-next. Graph grew 5→7 steps (2 new corpus-grounded prerequisite steps); new `POST .../steps/{id}/complete` endpoint (READY-only guard, in-memory mutation); new `app/recommend.py` (zero-LLM ranking); `ResolvedPlan.recommended_step_id` wired through Home (Next-up card + suggestion inside Add Task) and Tracker (per-step Mark done). Caught and fixed a real pre-existing bug along the way: `/plan` used to 502 entirely on explanation failure, which would've silently broken this whole feature since it's explicitly meant to work without the key — now degrades gracefully. `eval_personas.py` (32/32) and `verify_resolver.py` (incl. a new walkthrough section) updated and passing; full loop also verified with a real Playwright browser run. See DECISIONS.md for the complete breakdown.
- [x] **Phase 5d (unplanned)** — Rebrand + product-judgment cleanup: Copilot → FinanceOne (new favicon, tagline), forced light mode (no more `dark:` anywhere), orange → deliberately-muted blue accent, Hindi/multilingual removed entirely (not hidden — see DECISIONS.md), real per-step action links (ISSS Portal, Aggie Card, Residence Life, SSA locator — real URLs, sourced from the Phase 3 corpus or freshly verified), mock seeded documents (admission letters for both personas, job offer letter for Priya, clearly marked as samples). All typechecked, linted, built, and screenshotted incl. with OS dark mode forced to confirm it's truly inert. Recovered cleanly from a self-inflicted whitespace-corruption bug mid-task (see DECISIONS.md).
- [x] **Phase 5e (unplanned, submission day)** — Full DESIGN.md implementation: `/plan` split into fast-deterministic + slow-explanations endpoints (real async architecture, not just a spinner), per-card loading state with `lucide-react` spinner + fade-in, blocked-step restyle (dashed/muted card + amber "Blocked by:" callout), animated SVG progress ring, two-column Home layout, Docs page as a card grid with real drag-and-drop, and shadcn/ui for the account switcher. shadcn's init silently broke the background/font/dark-mode setup on install — caught and fixed same session (see DECISIONS.md). All typechecked, linted, built clean, and verified live incl. a corrected mid-fetch screenshot proving the spinner→fade-in actually happens.
- [ ] **Phase 8** — Demo/pitch hardening (remaining buffer, now ~3–4 hrs)

Total budget: ~16–20 hrs, solo, submission Aug 16.

## NOT-DOING

See DECISIONS.md — kept there as the single source of truth so it doesn't drift out of
sync between the two files.
